To run application you should create Mysql database 'books'.

New database credentials should be placed in application.properties.

Liquibase creates some books, authors and genres automatically.

Application runs at port 8080.